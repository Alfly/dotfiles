- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''  # PROJ HERE
    ~/.shell:
    ~/.zshrc:
    ~/.zsh:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.fzf:

- shell: 
  - git submodule sync --recursive

